name: FastAPI Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: fastapi_user
          POSTGRES_PASSWORD: FastAPI123
          POSTGRES_DB: fastapi_test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U fastapi_user -d fastapi_test_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://fastapi_user:FastAPI123@localhost:5432/fastapi_test_db
      TEST_DATABASE_URL: postgresql://fastapi_user:FastAPI123@localhost:5432/fastapi_test_db

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Check PostgreSQL
        run: pg_isready -h localhost -p 5432 -U fastapi_user -d fastapi_test_db

      - name: Run tests
        run: pytest -v

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: docker build -t cheemarlamaheshwarreddy/fastapi-app:latest .

      - name: Push Docker image
        run: docker push cheemarlamaheshwarreddy/fastapi-app:latest
